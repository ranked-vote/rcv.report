# Git LFS configuration for rcv.report

# Archive files (compressed election data)
# These are our source of truth, committed to git via LFS
report_pipeline/archives/**/*.tar.xz filter=lfs diff=lfs merge=lfs -text

# Legacy: raw-data is now gitignored, but keeping these for reference
# report_pipeline/raw-data/ is the working directory (not committed)
